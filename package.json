{
  "name": "harbourline-compliance-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static Singapore family office compliance dashboard prototype.",
  "scripts": {
    "build": "node --check app.js && node scripts/build.mjs",
    "check": "node --check app.js",
    "dev": "python3 -m http.server 4174 --bind 127.0.0.1"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  }
}
